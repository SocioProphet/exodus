# Publish plan

## Intended repository

- Org: `SocioProphet`
- Repo: `exodus`

## Current observed GitHub state

A separate public `SociOS-Linux/EXODUS` repository may already exist in connected GitHub surfaces.
That repository should be treated as lineage context, not as the default destination for this scaffold, unless we explicitly decide to reuse it.

## Recommended publication path

1. create `SocioProphet/exodus`
2. publish this scaffold as the initial commit
3. import or reference useful lineage from other EXODUS repos deliberately, not implicitly
